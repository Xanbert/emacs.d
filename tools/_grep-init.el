;;;; _grep-init --- grep tools config

;;; _grep-init.el
;;; Commentary:

;; This file contains config of ag and ripgrep

;;; Code:

(use-package ag
  :ensure t)

(use-package ripgrep
  :ensure t)

(provide '_grep-init)
;;; _grep-init.el ends here
